{
    "definitions": {},
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "title": "update schema",
    "required": ["id", "title", "text"],
    "properties": {
      "id": { "type": "string", "pattern": "^[a-zA-Z0-9]+$" },
      "title": { "type": "string", "pattern": "^[a-zA-Z0-9]+$" },
      "text": { "type": "string", "pattern": "^[a-zA-Z0-9]+$" }
    }
  }